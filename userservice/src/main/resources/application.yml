spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 8081
  
# App Properties
weather.app.jwtSecret: jwtWeatherApp.comSecretKey
weather.app.jwtExpiration: 86400